version: '3'

services:
  chain:
    build:
      context: .
    environment:
      ChainConfig: "chain.proxyminer.toml"
    expose:
      - 8801
      - 8545

  chain32:
    build:
      context: .
    environment:
      ChainConfig: "chain.proxyminer.toml"
    expose:
        - 8801

  chain31:
    build:
      context: .
    environment:
      ChainConfig: "chain.proxyminer.toml"
    expose:
      - 8801
  chain30:
    build:
      context: .
    environment:
      ChainConfig: "chain.proxyminer.toml"
    expose:
      - 8801
      - 8545