version: '3'

services:
  main:
    build:
      context: .
    environment:
      ChainConfig: "chain.test.toml"
#    expose:
#      - "8802"

  para1:
    build:
      context: .
    environment:
      ChainConfig: "chain.para1.toml"

  para2:
    build:
      context: .
    environment:
      ChainConfig: "chain.para2.toml"
